module.exports = {
  // Near match Jaccard similarity threshold (0.75 = 75%)
  NEAR_MATCH_THRESHOLD: 0.75,
  // Minimum passage character length for comparison
  MIN_PASSAGE_CHAR_LENGTH: 30,
  // Maximum supported documents per analysis
  MAX_SUPPORTED_DOCUMENTS: 10,
  // Minimum required documents per analysis
  MIN_REQUIRED_DOCUMENTS: 2,
};
